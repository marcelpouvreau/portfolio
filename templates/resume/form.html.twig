{% extends 'base.html.twig' %}

{% block title %}Resume Form!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Resume</h1>
    <ul>
        {{ form_start(resumeForm) }}
            {{ form_row(resumeForm.file) }}
            {{ form_row(resumeForm.name) }}
        {{ form_end(resumeForm) }}
    </ul>
</div>
{% endblock %}
